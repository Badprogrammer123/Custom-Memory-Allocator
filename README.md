# Custom-Memory-Allocator
Implemented a custom memory allocator in C with mmap, inspired by the behavior of malloc. Designed and implemented built block metadata structures, supports allocation, deallocation, block splitting, coalescing, and reallocation without relying on external libraries.
